# Internet Gateways

```scss
Region
   │
   └── VPC
        │
        ├── Internet Gateway (attached to VPC)
        │
        ├── Subnet 1
        ├── Subnet 2
        └── Subnet 3
```

- Internet Gateway (IGW) is attached to a VPC, not to a subnet.
- Subnets use the IGW through **route tables**

